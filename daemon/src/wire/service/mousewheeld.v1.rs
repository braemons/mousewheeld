// This file is @generated by prost-build.
/// Generated server implementations.
pub mod calibration_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with CalibrationServer.
    #[async_trait]
    pub trait Calibration: std::marker::Send + std::marker::Sync + 'static {
        async fn read_calibration(
            &self,
            request: tonic::Request<crate::wire::ReadCalibrationRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::CalibrationState>,
            tonic::Status,
        >;
        /// Change one or more axes. Absent fields are left alone, so a panel may send
        /// a single number. An axis name the rig does not have is a 404, never a new
        /// axis.
        async fn replace_calibration(
            &self,
            request: tonic::Request<crate::wire::CalibrationPatch>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::CalibrationState>,
            tonic::Status,
        >;
        /// The 2-D ball. Answers 501 until the hardware exists — declared so that
        /// adding it is filling in rather than redesigning.
        async fn read_ball(
            &self,
            request: tonic::Request<crate::wire::ReadBallRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::BallCalibration>,
            tonic::Status,
        >;
        async fn replace_ball(
            &self,
            request: tonic::Request<crate::wire::BallCalibration>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::BallCalibration>,
            tonic::Status,
        >;
        /// Begin counting for a guided measurement.
        ///
        /// **The measurement is the point.** `counts_per_rev / (π · diameter_cm)`
        /// describes a wheel the animal does not run on: what a corridor position is
        /// made of is how far the feet travelled, on the surface they touch, with
        /// whatever tread is on it — a few percent off the nominal every time.
        async fn start_measuring(
            &self,
            request: tonic::Request<crate::wire::StartMeasurement>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::MeasurementStarted>,
            tonic::Status,
        >;
        /// Stop counting and report what the wheel said, beside what the config says.
        /// Nothing is changed by this call.
        async fn finish_measuring(
            &self,
            request: tonic::Request<crate::wire::FinishMeasuringRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::MeasurementResult>,
            tonic::Status,
        >;
        /// Apply the last measurement.
        ///
        /// A separate, deliberate press, because a calibration change invalidates
        /// every compiled zone set: a set armed under one calibration is never
        /// silently reinterpreted under another.
        async fn apply_measurement(
            &self,
            request: tonic::Request<crate::wire::ApplyMeasurementRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::MeasurementApplied>,
            tonic::Status,
        >;
    }
    /// Counts per centimetre, and how it was measured.
    ///
    /// **This daemon owns the calibration.** The firmware knows counts and nothing
    /// else; vstimd is told centimetres and restates nothing; triald needs
    /// centimetres too and cannot read either one's config. One copy, here, and
    /// every number that leaves is in centimetres.
    #[derive(Debug)]
    pub struct CalibrationServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> CalibrationServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for CalibrationServer<T>
    where
        T: Calibration,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/mousewheeld.v1.Calibration/ReadCalibration" => {
                    #[allow(non_camel_case_types)]
                    struct ReadCalibrationSvc<T: Calibration>(pub Arc<T>);
                    impl<
                        T: Calibration,
                    > tonic::server::UnaryService<crate::wire::ReadCalibrationRequest>
                    for ReadCalibrationSvc<T> {
                        type Response = crate::wire::CalibrationState;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadCalibrationRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Calibration>::read_calibration(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadCalibrationSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Calibration/ReplaceCalibration" => {
                    #[allow(non_camel_case_types)]
                    struct ReplaceCalibrationSvc<T: Calibration>(pub Arc<T>);
                    impl<
                        T: Calibration,
                    > tonic::server::UnaryService<crate::wire::CalibrationPatch>
                    for ReplaceCalibrationSvc<T> {
                        type Response = crate::wire::CalibrationState;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::CalibrationPatch>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Calibration>::replace_calibration(&inner, request)
                                    .await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReplaceCalibrationSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Calibration/ReadBall" => {
                    #[allow(non_camel_case_types)]
                    struct ReadBallSvc<T: Calibration>(pub Arc<T>);
                    impl<
                        T: Calibration,
                    > tonic::server::UnaryService<crate::wire::ReadBallRequest>
                    for ReadBallSvc<T> {
                        type Response = crate::wire::BallCalibration;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadBallRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Calibration>::read_ball(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadBallSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Calibration/ReplaceBall" => {
                    #[allow(non_camel_case_types)]
                    struct ReplaceBallSvc<T: Calibration>(pub Arc<T>);
                    impl<
                        T: Calibration,
                    > tonic::server::UnaryService<crate::wire::BallCalibration>
                    for ReplaceBallSvc<T> {
                        type Response = crate::wire::BallCalibration;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::BallCalibration>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Calibration>::replace_ball(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReplaceBallSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Calibration/StartMeasuring" => {
                    #[allow(non_camel_case_types)]
                    struct StartMeasuringSvc<T: Calibration>(pub Arc<T>);
                    impl<
                        T: Calibration,
                    > tonic::server::UnaryService<crate::wire::StartMeasurement>
                    for StartMeasuringSvc<T> {
                        type Response = crate::wire::MeasurementStarted;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::StartMeasurement>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Calibration>::start_measuring(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = StartMeasuringSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Calibration/FinishMeasuring" => {
                    #[allow(non_camel_case_types)]
                    struct FinishMeasuringSvc<T: Calibration>(pub Arc<T>);
                    impl<
                        T: Calibration,
                    > tonic::server::UnaryService<crate::wire::FinishMeasuringRequest>
                    for FinishMeasuringSvc<T> {
                        type Response = crate::wire::MeasurementResult;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::FinishMeasuringRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Calibration>::finish_measuring(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = FinishMeasuringSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Calibration/ApplyMeasurement" => {
                    #[allow(non_camel_case_types)]
                    struct ApplyMeasurementSvc<T: Calibration>(pub Arc<T>);
                    impl<
                        T: Calibration,
                    > tonic::server::UnaryService<crate::wire::ApplyMeasurementRequest>
                    for ApplyMeasurementSvc<T> {
                        type Response = crate::wire::MeasurementApplied;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ApplyMeasurementRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Calibration>::apply_measurement(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ApplyMeasurementSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for CalibrationServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "mousewheeld.v1.Calibration";
    impl<T> tonic::server::NamedService for CalibrationServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
/// Generated server implementations.
pub mod config_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with ConfigServer.
    #[async_trait]
    pub trait Config: std::marker::Send + std::marker::Sync + 'static {
        /// The settings a session may change and the ones it may only read, in one
        /// answer.
        async fn read_config(
            &self,
            request: tonic::Request<crate::wire::ReadConfigRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ConfigView>,
            tonic::Status,
        >;
        /// Change the rates. Absent fields are left alone.
        async fn patch_config(
            &self,
            request: tonic::Request<crate::wire::ConfigPatch>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ConfigView>,
            tonic::Status,
        >;
        /// The output lines, by name.
        ///
        /// Read-only over the API: a zone set says `"line": "zone_goal"`, and the pin
        /// behind that name belongs to the rig's config. This is the same split
        /// statemachined draws between its graphs and its line map.
        async fn read_lines(
            &self,
            request: tonic::Request<crate::wire::ReadLinesRequest>,
        ) -> std::result::Result<tonic::Response<crate::wire::LineMap>, tonic::Status>;
    }
    /// Rates, and the line map.
    ///
    /// **Two kinds of setting, and the split is deliberate.** The wiring — which
    /// pin is `zone_goal`, which port the board is on — is changed where wiring is
    /// described, in the rig config file, and is not writable over this API. The
    /// rates are changed per session, by a person at a console, and are.
    #[derive(Debug)]
    pub struct ConfigServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> ConfigServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for ConfigServer<T>
    where
        T: Config,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/mousewheeld.v1.Config/ReadConfig" => {
                    #[allow(non_camel_case_types)]
                    struct ReadConfigSvc<T: Config>(pub Arc<T>);
                    impl<
                        T: Config,
                    > tonic::server::UnaryService<crate::wire::ReadConfigRequest>
                    for ReadConfigSvc<T> {
                        type Response = crate::wire::ConfigView;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadConfigRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Config>::read_config(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadConfigSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Config/PatchConfig" => {
                    #[allow(non_camel_case_types)]
                    struct PatchConfigSvc<T: Config>(pub Arc<T>);
                    impl<T: Config> tonic::server::UnaryService<crate::wire::ConfigPatch>
                    for PatchConfigSvc<T> {
                        type Response = crate::wire::ConfigView;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ConfigPatch>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Config>::patch_config(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = PatchConfigSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Config/ReadLines" => {
                    #[allow(non_camel_case_types)]
                    struct ReadLinesSvc<T: Config>(pub Arc<T>);
                    impl<
                        T: Config,
                    > tonic::server::UnaryService<crate::wire::ReadLinesRequest>
                    for ReadLinesSvc<T> {
                        type Response = crate::wire::LineMap;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadLinesRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Config>::read_lines(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadLinesSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for ConfigServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "mousewheeld.v1.Config";
    impl<T> tonic::server::NamedService for ConfigServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
/// Generated server implementations.
pub mod device_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with DeviceServer.
    #[async_trait]
    pub trait Device: std::marker::Send + std::marker::Sync + 'static {
        /// What this daemon is and what it speaks. Answers without a board.
        async fn read_version(
            &self,
            request: tonic::Request<crate::wire::ReadVersionRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::VersionReport>,
            tonic::Status,
        >;
        /// Whether a board is attached, which one, and how the link is holding up.
        async fn read_device(
            &self,
            request: tonic::Request<crate::wire::ReadDeviceRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::DeviceInfo>,
            tonic::Status,
        >;
        /// Open the serial port named in the rig config and greet the board.
        ///
        /// Named `OpenLink` and not `Connect` because a generated client already has
        /// a `connect` — the one that opens the channel — and the two collide.
        ///
        /// Idempotent: connecting a connected daemon answers with what is already
        /// there. Refuses `no_device` when the port is not openable, and refuses a
        /// board whose protocol is below the floor **by name**, rather than speaking
        /// to it half-understood.
        async fn open_link(
            &self,
            request: tonic::Request<crate::wire::OpenLinkRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::DeviceInfo>,
            tonic::Status,
        >;
        /// What the board is running, and what this daemon could flash onto it.
        async fn read_firmware(
            &self,
            request: tonic::Request<crate::wire::ReadFirmwareRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::FirmwareVersions>,
            tonic::Status,
        >;
        /// The recent wire, both directions, uninterpreted.
        ///
        /// A bounded ring. The conversation and the sample traffic are kept apart —
        /// at 500 Hz a shared ring evicts the `hello` handshake within seconds, which
        /// is exactly the part somebody reads a wire log to see.
        async fn read_wire_log(
            &self,
            request: tonic::Request<crate::wire::ReadWireLogRequest>,
        ) -> std::result::Result<tonic::Response<crate::wire::WireLog>, tonic::Status>;
        /// Server streaming response type for the WatchWire method.
        type WatchWireStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<crate::wire::WireLine, tonic::Status>,
            >
            + std::marker::Send
            + 'static;
        /// The same wire as it happens.
        async fn watch_wire(
            &self,
            request: tonic::Request<crate::wire::WatchWireRequest>,
        ) -> std::result::Result<tonic::Response<Self::WatchWireStream>, tonic::Status>;
    }
    /// The board, and the wire to it.
    ///
    /// The first question anybody asks this daemon is whether a board is attached,
    /// and the second is which firmware is on it.
    #[derive(Debug)]
    pub struct DeviceServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> DeviceServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for DeviceServer<T>
    where
        T: Device,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/mousewheeld.v1.Device/ReadVersion" => {
                    #[allow(non_camel_case_types)]
                    struct ReadVersionSvc<T: Device>(pub Arc<T>);
                    impl<
                        T: Device,
                    > tonic::server::UnaryService<crate::wire::ReadVersionRequest>
                    for ReadVersionSvc<T> {
                        type Response = crate::wire::VersionReport;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadVersionRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Device>::read_version(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadVersionSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Device/ReadDevice" => {
                    #[allow(non_camel_case_types)]
                    struct ReadDeviceSvc<T: Device>(pub Arc<T>);
                    impl<
                        T: Device,
                    > tonic::server::UnaryService<crate::wire::ReadDeviceRequest>
                    for ReadDeviceSvc<T> {
                        type Response = crate::wire::DeviceInfo;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadDeviceRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Device>::read_device(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadDeviceSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Device/OpenLink" => {
                    #[allow(non_camel_case_types)]
                    struct OpenLinkSvc<T: Device>(pub Arc<T>);
                    impl<
                        T: Device,
                    > tonic::server::UnaryService<crate::wire::OpenLinkRequest>
                    for OpenLinkSvc<T> {
                        type Response = crate::wire::DeviceInfo;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::OpenLinkRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Device>::open_link(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = OpenLinkSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Device/ReadFirmware" => {
                    #[allow(non_camel_case_types)]
                    struct ReadFirmwareSvc<T: Device>(pub Arc<T>);
                    impl<
                        T: Device,
                    > tonic::server::UnaryService<crate::wire::ReadFirmwareRequest>
                    for ReadFirmwareSvc<T> {
                        type Response = crate::wire::FirmwareVersions;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadFirmwareRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Device>::read_firmware(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadFirmwareSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Device/ReadWireLog" => {
                    #[allow(non_camel_case_types)]
                    struct ReadWireLogSvc<T: Device>(pub Arc<T>);
                    impl<
                        T: Device,
                    > tonic::server::UnaryService<crate::wire::ReadWireLogRequest>
                    for ReadWireLogSvc<T> {
                        type Response = crate::wire::WireLog;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadWireLogRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Device>::read_wire_log(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadWireLogSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Device/WatchWire" => {
                    #[allow(non_camel_case_types)]
                    struct WatchWireSvc<T: Device>(pub Arc<T>);
                    impl<
                        T: Device,
                    > tonic::server::ServerStreamingService<
                        crate::wire::WatchWireRequest,
                    > for WatchWireSvc<T> {
                        type Response = crate::wire::WireLine;
                        type ResponseStream = T::WatchWireStream;
                        type Future = BoxFuture<
                            tonic::Response<Self::ResponseStream>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::WatchWireRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Device>::watch_wire(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = WatchWireSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.server_streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for DeviceServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "mousewheeld.v1.Device";
    impl<T> tonic::server::NamedService for DeviceServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
/// Generated server implementations.
pub mod state_service_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with StateServiceServer.
    #[async_trait]
    pub trait StateService: std::marker::Send + std::marker::Sync + 'static {
        /// Everything true right now, per axis, with the link health that says
        /// whether to believe it.
        async fn read_state(
            &self,
            request: tonic::Request<crate::wire::ReadStateRequest>,
        ) -> std::result::Result<tonic::Response<crate::wire::RigState>, tonic::Status>;
        /// Server streaming response type for the WatchState method.
        type WatchStateStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<crate::wire::StreamFrame, tonic::Status>,
            >
            + std::marker::Send
            + 'static;
        /// The decimated path, as it happens.
        ///
        /// `rate_hz` is a query parameter and is what the browser asks for, **not**
        /// what the device samples at: the daemon decimates and says what it lost.
        /// A consumer breaks its line on `lost_before` and on nothing else.
        async fn watch_state(
            &self,
            request: tonic::Request<crate::wire::WatchStateRequest>,
        ) -> std::result::Result<tonic::Response<Self::WatchStateStream>, tonic::Status>;
        /// Move the **API origin** — what displacement, distance and the zones are
        /// measured from.
        ///
        /// Never the accumulator published to a camera: vstimd differences that every
        /// frame, and a value that jumps backwards jumps the corridor backwards.
        /// Teleporting a corridor is a command to vstimd, from whoever runs the
        /// experiment.
        async fn zero_position(
            &self,
            request: tonic::Request<crate::wire::ZeroRequest>,
        ) -> std::result::Result<tonic::Response<crate::wire::RigState>, tonic::Status>;
        /// Set the **API origin** to a specific position.
        ///
        /// This allows vstimd to sync the wheel to an arbitrary position, e.g. when
        /// resetting a corridor scene. Unlike `ZeroPosition` which sets the origin
        /// to the current position, `SetPosition` allows you to specify any position
        /// in centimetres. The origin is adjusted so that `position_cm` matches the
        /// requested value.
        ///
        /// The firmware does not support absolute positioning; the daemon computes
        /// the required origin offset and sends it to the device.
        async fn set_position(
            &self,
            request: tonic::Request<crate::wire::SetPositionRequest>,
        ) -> std::result::Result<tonic::Response<crate::wire::RigState>, tonic::Status>;
    }
    /// Where the wheel is.
    #[derive(Debug)]
    pub struct StateServiceServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> StateServiceServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for StateServiceServer<T>
    where
        T: StateService,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/mousewheeld.v1.StateService/ReadState" => {
                    #[allow(non_camel_case_types)]
                    struct ReadStateSvc<T: StateService>(pub Arc<T>);
                    impl<
                        T: StateService,
                    > tonic::server::UnaryService<crate::wire::ReadStateRequest>
                    for ReadStateSvc<T> {
                        type Response = crate::wire::RigState;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadStateRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as StateService>::read_state(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadStateSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.StateService/WatchState" => {
                    #[allow(non_camel_case_types)]
                    struct WatchStateSvc<T: StateService>(pub Arc<T>);
                    impl<
                        T: StateService,
                    > tonic::server::ServerStreamingService<
                        crate::wire::WatchStateRequest,
                    > for WatchStateSvc<T> {
                        type Response = crate::wire::StreamFrame;
                        type ResponseStream = T::WatchStateStream;
                        type Future = BoxFuture<
                            tonic::Response<Self::ResponseStream>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::WatchStateRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as StateService>::watch_state(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = WatchStateSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.server_streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.StateService/ZeroPosition" => {
                    #[allow(non_camel_case_types)]
                    struct ZeroPositionSvc<T: StateService>(pub Arc<T>);
                    impl<
                        T: StateService,
                    > tonic::server::UnaryService<crate::wire::ZeroRequest>
                    for ZeroPositionSvc<T> {
                        type Response = crate::wire::RigState;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ZeroRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as StateService>::zero_position(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ZeroPositionSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.StateService/SetPosition" => {
                    #[allow(non_camel_case_types)]
                    struct SetPositionSvc<T: StateService>(pub Arc<T>);
                    impl<
                        T: StateService,
                    > tonic::server::UnaryService<crate::wire::SetPositionRequest>
                    for SetPositionSvc<T> {
                        type Response = crate::wire::RigState;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::SetPositionRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as StateService>::set_position(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = SetPositionSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for StateServiceServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "mousewheeld.v1.StateService";
    impl<T> tonic::server::NamedService for StateServiceServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
/// Generated server implementations.
pub mod zones_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with ZonesServer.
    #[async_trait]
    pub trait Zones: std::marker::Send + std::marker::Sync + 'static {
        /// The names in the store.
        async fn list_zone_sets(
            &self,
            request: tonic::Request<crate::wire::ListZoneSetsRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ZoneSetNames>,
            tonic::Status,
        >;
        /// One stored set, exactly as it is on disk.
        async fn read_zone_set(
            &self,
            request: tonic::Request<crate::wire::ZoneSetName>,
        ) -> std::result::Result<tonic::Response<crate::wire::ZoneSet>, tonic::Status>;
        /// Store a set. Refused if it does not parse or does not compile.
        async fn replace_zone_set(
            &self,
            request: tonic::Request<crate::wire::WriteZoneSet>,
        ) -> std::result::Result<tonic::Response<crate::wire::ZoneSet>, tonic::Status>;
        /// Compile a **draft** — what somebody is typing, before it is saved.
        ///
        /// The body goes through the daemon's own deserializer and compiler, so what
        /// comes back is the refusal the real thing would give. There is no second,
        /// looser description of a zone set in a browser to disagree with it.
        async fn validate_draft(
            &self,
            request: tonic::Request<crate::wire::ZoneSet>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ValidationReport>,
            tonic::Status,
        >;
        /// Compile the set as it is **stored** — "is that one still good after the
        /// calibration changed".
        async fn validate_zone_set(
            &self,
            request: tonic::Request<crate::wire::ZoneSetName>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ValidationReport>,
            tonic::Status,
        >;
        /// The JSON Schema of a zone set **file**, for an editor or a checker.
        ///
        /// Not the schema of `ZoneSet` above — that one is this file, and a client
        /// generates from it. This is the stored spelling: `"$goal_cm"` for a
        /// reference, `"displacement"` for a metric, which is what somebody has open
        /// in an editor. Arbitrary JSON, so it is carried as such.
        ///
        /// Committed in this repository as well, at
        /// `docs/reference/zone-set.schema.json`, because an editor and CI must reach
        /// it without a daemon running.
        async fn read_zone_set_schema(
            &self,
            request: tonic::Request<crate::wire::ReadZoneSetSchemaRequest>,
        ) -> std::result::Result<tonic::Response<::prost_types::Struct>, tonic::Status>;
        /// The same three, on the **file** rather than the message.
        ///
        /// A zone set has two spellings and always has: the message above, which a
        /// generated client builds, and the file on disk, which a person edits and
        /// copies between rigs. `ReadZoneSet` gives the first. These give the second,
        /// as the text it is, because an editor with a cursor in it is editing bytes —
        /// and a browser that converted between the two spellings itself would be the
        /// second, looser description of a zone set that this service exists to avoid.
        ///
        /// The daemon parses with its own deserializer, so what comes back is the
        /// refusal a hand-edited file would earn, with the line and column in it.
        async fn read_zone_set_file(
            &self,
            request: tonic::Request<crate::wire::ZoneSetName>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ZoneSetFile>,
            tonic::Status,
        >;
        /// Parse, compile, and store. Answers with the file as it was written, which
        /// is the canonical spelling — an editor shows what is on disk, not what was
        /// typed at it.
        async fn write_zone_set_file(
            &self,
            request: tonic::Request<crate::wire::ZoneSetFile>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ZoneSetFile>,
            tonic::Status,
        >;
        /// Parse and compile a file that is not in the store. `name` may be empty:
        /// this is a draft.
        async fn validate_zone_set_file(
            &self,
            request: tonic::Request<crate::wire::ZoneSetFile>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ValidationReport>,
            tonic::Status,
        >;
        /// What is armed, what fired, and where.
        async fn read_armed(
            &self,
            request: tonic::Request<crate::wire::ReadArmedRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ArmedZones>,
            tonic::Status,
        >;
        /// Resolve the patch, compile against the calibration in force, upload, and
        /// wait for the board to answer `armed`.
        ///
        /// Refuses when the set does not fit, naming what overflowed; refuses a
        /// `$name` the patch does not carry, naming the name. Never a default — a
        /// goal distance that silently became zero is a trial that looks like it ran.
        async fn arm(
            &self,
            request: tonic::Request<crate::wire::ArmRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ArmedZones>,
            tonic::Status,
        >;
        async fn disarm(
            &self,
            request: tonic::Request<crate::wire::DisarmRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::ArmedZones>,
            tonic::Status,
        >;
        /// Write what is armed to the board's flash, so a standalone rig comes up
        /// with its zones. Refuses `nothing_armed`.
        async fn save_to_flash(
            &self,
            request: tonic::Request<crate::wire::SaveToFlashRequest>,
        ) -> std::result::Result<
            tonic::Response<crate::wire::DeviceInfo>,
            tonic::Status,
        >;
    }
    /// Distances at which a line fires.
    ///
    /// **Zones are dynamic; the lines they drive are not.** A zone set names
    /// `"line": "zone_goal"` and never a pin, so one set runs on every rig that has
    /// a `zone_goal` line and a rewired rig edits one file.
    ///
    /// **Authored in centimetres, compiled to counts.** The firmware has no
    /// floating point on any path whose result crosses the wire, so the host turns
    /// every centimetre into an integer interval before the board sees it — against
    /// a named calibration, which is why changing the calibration marks compiled
    /// sets stale.
    #[derive(Debug)]
    pub struct ZonesServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> ZonesServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for ZonesServer<T>
    where
        T: Zones,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/mousewheeld.v1.Zones/ListZoneSets" => {
                    #[allow(non_camel_case_types)]
                    struct ListZoneSetsSvc<T: Zones>(pub Arc<T>);
                    impl<
                        T: Zones,
                    > tonic::server::UnaryService<crate::wire::ListZoneSetsRequest>
                    for ListZoneSetsSvc<T> {
                        type Response = crate::wire::ZoneSetNames;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ListZoneSetsRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::list_zone_sets(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ListZoneSetsSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ReadZoneSet" => {
                    #[allow(non_camel_case_types)]
                    struct ReadZoneSetSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::ZoneSetName>
                    for ReadZoneSetSvc<T> {
                        type Response = crate::wire::ZoneSet;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ZoneSetName>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::read_zone_set(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadZoneSetSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ReplaceZoneSet" => {
                    #[allow(non_camel_case_types)]
                    struct ReplaceZoneSetSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::WriteZoneSet>
                    for ReplaceZoneSetSvc<T> {
                        type Response = crate::wire::ZoneSet;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::WriteZoneSet>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::replace_zone_set(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReplaceZoneSetSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ValidateDraft" => {
                    #[allow(non_camel_case_types)]
                    struct ValidateDraftSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::ZoneSet>
                    for ValidateDraftSvc<T> {
                        type Response = crate::wire::ValidationReport;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ZoneSet>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::validate_draft(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ValidateDraftSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ValidateZoneSet" => {
                    #[allow(non_camel_case_types)]
                    struct ValidateZoneSetSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::ZoneSetName>
                    for ValidateZoneSetSvc<T> {
                        type Response = crate::wire::ValidationReport;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ZoneSetName>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::validate_zone_set(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ValidateZoneSetSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ReadZoneSetSchema" => {
                    #[allow(non_camel_case_types)]
                    struct ReadZoneSetSchemaSvc<T: Zones>(pub Arc<T>);
                    impl<
                        T: Zones,
                    > tonic::server::UnaryService<crate::wire::ReadZoneSetSchemaRequest>
                    for ReadZoneSetSchemaSvc<T> {
                        type Response = ::prost_types::Struct;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<
                                crate::wire::ReadZoneSetSchemaRequest,
                            >,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::read_zone_set_schema(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadZoneSetSchemaSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ReadZoneSetFile" => {
                    #[allow(non_camel_case_types)]
                    struct ReadZoneSetFileSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::ZoneSetName>
                    for ReadZoneSetFileSvc<T> {
                        type Response = crate::wire::ZoneSetFile;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ZoneSetName>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::read_zone_set_file(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadZoneSetFileSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/WriteZoneSetFile" => {
                    #[allow(non_camel_case_types)]
                    struct WriteZoneSetFileSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::ZoneSetFile>
                    for WriteZoneSetFileSvc<T> {
                        type Response = crate::wire::ZoneSetFile;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ZoneSetFile>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::write_zone_set_file(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = WriteZoneSetFileSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ValidateZoneSetFile" => {
                    #[allow(non_camel_case_types)]
                    struct ValidateZoneSetFileSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::ZoneSetFile>
                    for ValidateZoneSetFileSvc<T> {
                        type Response = crate::wire::ValidationReport;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ZoneSetFile>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::validate_zone_set_file(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ValidateZoneSetFileSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/ReadArmed" => {
                    #[allow(non_camel_case_types)]
                    struct ReadArmedSvc<T: Zones>(pub Arc<T>);
                    impl<
                        T: Zones,
                    > tonic::server::UnaryService<crate::wire::ReadArmedRequest>
                    for ReadArmedSvc<T> {
                        type Response = crate::wire::ArmedZones;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ReadArmedRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::read_armed(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadArmedSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/Arm" => {
                    #[allow(non_camel_case_types)]
                    struct ArmSvc<T: Zones>(pub Arc<T>);
                    impl<T: Zones> tonic::server::UnaryService<crate::wire::ArmRequest>
                    for ArmSvc<T> {
                        type Response = crate::wire::ArmedZones;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::ArmRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::arm(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ArmSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/Disarm" => {
                    #[allow(non_camel_case_types)]
                    struct DisarmSvc<T: Zones>(pub Arc<T>);
                    impl<
                        T: Zones,
                    > tonic::server::UnaryService<crate::wire::DisarmRequest>
                    for DisarmSvc<T> {
                        type Response = crate::wire::ArmedZones;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::DisarmRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::disarm(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = DisarmSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/mousewheeld.v1.Zones/SaveToFlash" => {
                    #[allow(non_camel_case_types)]
                    struct SaveToFlashSvc<T: Zones>(pub Arc<T>);
                    impl<
                        T: Zones,
                    > tonic::server::UnaryService<crate::wire::SaveToFlashRequest>
                    for SaveToFlashSvc<T> {
                        type Response = crate::wire::DeviceInfo;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<crate::wire::SaveToFlashRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as Zones>::save_to_flash(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = SaveToFlashSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for ZonesServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "mousewheeld.v1.Zones";
    impl<T> tonic::server::NamedService for ZonesServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
